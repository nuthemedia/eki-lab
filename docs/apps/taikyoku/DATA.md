# Data

- `binaryForms(depth)`は新しい爻を上に加え、陽→陰の順で全組み合わせを返す。
- `binaryForms(3)`は`domain/iching/hexagrams.ts`の`TRIGRAMS`と同じ順になる。
- 六十四卦は下卦3本の後に上卦3本を連結する。
- 64形の表示は8×8のインスタンス行列へ変換し、個別意味や名称は保持しない。
- カメラは正規化進行`0…1`から決定的に算出する。
